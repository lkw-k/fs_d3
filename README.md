# fs_d3

방명록 풀스택 프로젝트.

## 브랜치 구조

- `back`: 백엔드 (Node.js/Express + MySQL)
- `front`: 프론트엔드 (Vite)

각 브랜치에 해당 프로젝트만 포함되어 있습니다.

```bash
git checkout back    # 백엔드 코드로 전환
git checkout front   # 프론트 코드로 전환
```
